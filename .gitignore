# Purpose: Ignore IDE, build and generated files so collaborators don't commit per‑machine output or IDE configs.
# This keeps the repo small and avoids conflicts caused by different local build/output folders.

# Ignore compiled output and build directories
/out/
/**/out/
/build/
/target/
/bin/

# Ignore IDE files (Eclipse, IntelliJ, VSCode)
.classpath
.project
.settings/
.idea/
*.iml
.vscode/

# Ignore OS files
.DS_Store
Thumbs.db

# Ignore Java class and jar files
*.class
*.jar

# If using Gradle/Maven, ignore wrapper and caches if preferred
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Optional: ignore logs and temporary files
*.log
*.tmp
